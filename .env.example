# Copy to .env and fill in. .env is gitignored — never commit it.
# The kit itself needs no keys. These are here because most people
# end up wiring at least one search or research API into a skill.

# SEARCH_API_KEY=
# RESEARCH_API_KEY=
